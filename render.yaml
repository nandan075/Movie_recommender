services:
  - type: web
    name: movie-recommender
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 600 --workers 1 --preload
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
